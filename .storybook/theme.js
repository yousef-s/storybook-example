import { create } from '@storybook/theming/create';

export default create({
  base: 'light',

  brandTitle: 'Web Components Library',
  brandUrl: 'https://github.com/yousef-s/storybook-example',
});
